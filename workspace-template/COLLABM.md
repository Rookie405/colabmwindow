# Project instructions for collabm

This file is added to the system prompt of every conversation started in this folder
(like CLAUDE.md). Edit freely, or append a line from inside the chat with `/memory <text>`.

- Reply in the language the user writes in; keep code, identifiers and file contents in English.
- Be concise. Prefer concrete commands and code over general advice.
- Files the user attaches appear inside <file path=...> tags; refer to them by path.
